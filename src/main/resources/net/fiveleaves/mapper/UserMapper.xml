<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.fiveleaves.mapper.UserMapper">

	<resultMap type="net.fiveleaves.domain.UserDTO" id="userMap">
	
		<id property="userNo" column="user_no"/>
		<result property="userNo" column="user_no"/>
		<result property="username" column="username"/>
		<result property="password" column="password"/>
		<result property="profile" column="profile"/>
		<result property="nickname" column="nickname"/>
		<result property="birth" column="birth"/>
		<result property="regdate" column="regdate"/>
		<result property="updateDate" column="updatedate"/>
		<collection property="authList" resultMap="authMap"/>
		
	</resultMap>
	
	<resultMap type="net.fiveleaves.domain.AuthorityDTO" id="authMap">
		<result property="userNo" column="user_no"/>
		<result property="authority" column="authority"/>
	</resultMap>
	
	<select id="read" resultMap="userMap">
	
		SELECT
			u.user_no, username, password, profile, nickname, birth, enabled, regdate, updatedate, authority
		FROM
			users u LEFT OUTER JOIN authorities a on u.user_no = a.user_no
		WHERE	username = #{username}
	
	</select>

</mapper>